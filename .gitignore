node_modules/
dist/
.env
*.tsbuildinfo

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
dev-debug.log
# Dependency directories
# Environment variables
# Editor directories and files
.idea
.vscode
.history
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# OS specific
.DS_Store

# Task files
tasks.json
tasks/

# Demo recording intermediates (final videos live in docs/assets/)
demo/videos/

# Test coverage
coverage/
.kody-engine/

# kody (single-session pipeline) runtime artifacts
# NOTE (stabilize fork): .kody/ is INTENTIONALLY tracked here so this fork
# can act as a kody-target (goal-tick reads .kody/goals/<id>/state.json,
# dashboard reads umbrella state, etc). Do NOT propagate this gitignore
# relaxation upstream to aharonyaircohen/kody-engine.
# .kody/
.kody-lean/

# Claude Code harness runtime artifacts (per-machine)
.claude/scheduled_tasks.lock
.claude/scheduled_tasks.log
